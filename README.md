# Atlas
Atlas is a modular back end framework, which I made for a specific project.

## Setup

To use Atlas simply pull the repository, and run:
```cmd
composer dumpautoload
```

Then to local host run:
```cmd
php Atlas localhost
```
